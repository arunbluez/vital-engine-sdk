/**
 * SDK version. Replays embed this and refuse to play against a mismatched
 * major version (balance/behaviour may have changed incompatibly).
 */
export const ENGINE_VERSION = '0.0.1'

/** Replay binary/format version (independent of the SDK version). */
export const REPLAY_FORMAT_VERSION = 1
